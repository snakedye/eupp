# Example .env for eupp-net
EUPP_PORT=3333

# REQUIRED: hex-encoded 32-byte ed25519 secret key (64 hex chars).
# You may also set SECRET_KEY instead of EUPP_SECRET_KEY as a fallback.
EUPP_SECRET_KEY=

# Enable mining? (true/false or 1/0). Defaults to false if omitted.
EUPP_MINING=true

# Number of blocks to fetch per sync chunk. Defaults to 16 if omitted.
EUPP_BLOCK_CHUNK_SIZE=16

# Indexing database configuration (used by eupp-db)
# Optional filesystem path where the index DB will be stored. If omitted,
# the DB will choose its own default location.
# Examples: './eupp-index-db', '/var/lib/eupp/index'
EUPP_INDEX_DB_PATH=./eupp-index-db

# Path to the block file where raw blocks are stored on-disk. Defaults to 'blocks.dat'
EUPP_BLOCK_FILE=blocks.dat
